<% if @to_do.valid? %>
	$('.js-to_dos').prepend("<%= escape_javascript(render(@to_do)) %>");
<% end %>
$('.new-to_do').html("<%= escape_javascript(render partial: 'to_dos/form', locals: { goal_id: @goal_id }) %>");
